insert:
INSERT INTO event (chat_id, json)
VALUES (?, ?::jsonb);

findRowId:
SELECT id
FROM event
WHERE chat_id = ?
AND json->>'id' = ?
LIMIT 1;

selectByChatIdAfter:
SELECT id, json::text AS json
FROM event
WHERE chat_id = ?
AND id > ?
ORDER BY id;
