selectByUsername:
SELECT *
FROM "user"
WHERE username = ?;

insert:
INSERT INTO "user" (username, password_hash)
VALUES (?, ?)
ON CONFLICT DO NOTHING
RETURNING id;

existsByUsername:
SELECT COUNT(*) > 0
FROM "user"
WHERE username = ?;
